/* 
    Member 3: Regular expression for binary strings with at least two zeros
    Language: L = {w | w contains at least two zeros (not necessarily consecutive)}
    NFA with exactly 6 states
*/
[0-1]*0[0-1]*0[0-1]*

/* Member name: Team Member 3 */